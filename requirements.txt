# requirements.txt
# Install with: pip install -r requirements.txt

opencv-python>=4.8.0
opencv-contrib-python>=4.8.0   # needed for cv2.face (LBPH recognizer)
numpy>=1.24.0
pandas>=2.0.0
openpyxl>=3.1.0                # Excel read/write + image embedding
Pillow>=10.0.0                  # Image handling (used by openpyxl for photo embed)
